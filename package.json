{
    "name": "structures",
    "version": "0.0.1",

    "repository": {
        "type": "git",
        "url": "https://github.com/shamasis/structures.git"
    },

    "devDependencies": {
        "jasmine-node": "*",
        "jshint": ">=2.10.2"
    },

    "scripts": {
        "test": "jshint src/ tests/ --verbose && jasmine-node tests/"
    }
}
